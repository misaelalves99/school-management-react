// src/types/Student.ts

export type Student = {
  id?: number; // opcional para criação
  name: string;
  email: string;
  dateOfBirth: string;
  enrollmentNumber: string;
  phone: string;
  address: string;
};
